import { QuoteHighlight } from "@/components/blog/ai-role-sharing/QuoteHighlight";

export function SourceMdQuoteHighlight() {
  return (
    <QuoteHighlight
      quote="入力素材の構造が、そのまま出力品質を変える"
      context="プロンプトの書き方ではなく、そもそも「何をAIに渡すか」の段階で決まっていた。"
    />
  );
}
